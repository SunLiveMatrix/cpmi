using System;
namespace cpmi.check
{
    public enum manpool
    {
        check, type, water, mapping, files, config, settings, typeSelect,
        founds, caches, hash, good, template, checkup, fits, rec, dec, play,
        stop, pause, factor, arms, physics, math, science, chemical, develop,
        form, usages, typeVector, vector, vectorPattern, vectorEvent, vectorNight,
        wave, wavelist, waveMapping, waveType, waveConfig, waveStates, waveLoop,
        current, currentFiles, currentBusiness, currentEvent, currentMap, 
        currentStates, currentToken
    }
}
