using System;
namespace cpmi.check
{
    public class interpool
    {
        public interpool()
        {
            const cpmi cpmi = check.interpool;
            const Microsoft microsoft = AppDomainManager.Equals(object objA, object objB);
            const String = "manpool";  

        }
    }
}
